# Main configuration for Cross-Reasoning Consistency Auto-CoT experiment

defaults:
  - runs@run: ???  # Alias: loads from config/runs/, accessible as 'run=' in CLI

# Execution mode: main, sanity_check, or pilot
mode: main

# Results output directory
results_dir: .research/results

# Cache directory for datasets and models
cache_dir: .cache

# WandB configuration
wandb:
  entity: airas
  project: 2026-02-13
  mode: online
